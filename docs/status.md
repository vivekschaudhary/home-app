# Project Status

_Last updated: 2026-06-22 — **WLT-24-2 (auto-detect subscriptions) story + design + copy READY** ([story](bets/WLT-24/stories/WLT-24-2/story.md)). The detection fast-follow: a **custom, provider-agnostic** `SubscriptionDetector` (the operator settled the Plaid-vs-custom elicitation → custom, so **no new Plaid product / no ADR-002 amendment**), reusing the WLT-24-1 cadence/merchant compute. It auto-marks recurring charges `source='auto'` — a **signal the human overrides** — gated high-precision (≥3 occurrences AND a clean cadence AND amount CV ≤ 0.10 AND confidence ≥ 0.70, "never alarming"). **Load-bearing decision: the dismissal model** — migration `0016` adds `dismissed_at`; **unmark becomes a durable soft-delete** and the detector **never re-adds a dismissed merchant** (a user "no" sticks); an explicit re-mark clears it (user > auto > dismissed). Runs in a new `detect-subscriptions` sync step **and** on first Subscriptions visit; the UI tags auto rows "detected" + a quiet review nudge. Plan: PR1 server (migration + detector + dismissal + write path + run-timing) → PR2 UI; Codex owns the `dismissed_at` RLS update + the detect→dismiss→no-re-add E2E. **Also today: FIX-2026-06-22 (empty Subscriptions panel) SHIPPED + closed out** — the `.in(dedup_keys)` URL-overflow that left the panel empty while flags persisted, fixed by paginating the read (PR #93, squash `d8439667`) + a deterministic source-guard; the closeout PR #94 (`4e186eb`) committed Codex's 180-charge regression E2E (it had landed **uncommitted** — the recurring pattern) and flipped the fix doc to shipped. Earlier: **WLT-24-1 fix SHIPPED: mark the MERCHANT, not just the one charge** ([FIX-2026-06-22](bets/WLT-24/stories/WLT-24-1/fixes/FIX-2026-06-22-mark-the-merchant.md), PR #91, squash `6e73ff9`). Operator dogfooding: marking a Netflix charge only flagged that row. Now one mark fans out to the whole merchant (entity-id-first match, the WLT-22-3/4 key) — every charge lights up, a sync step auto-joins new ones, and the monthly total infers from full history on the FIRST mark (the "mark 2 / cadence pending" friction is gone). No schema change; orthogonality preserved (the guard still passes). Codex CLEAN after one BLOCKER (the gated E2E had to prove the new merchant-mark contract — one mark → all flagged → total → sync auto-join of a name-variant charge → CDC + isolation); Codex updated it. CLEAR tied to HEAD `6e73ff9`. Earlier: **WLT-24-1 (See your subscriptions — mark a recurring charge) SHIPPED** (PR #89, squash `6f58636`). The Subscriptions surface is live: a `transaction_flags` overlay (owner-CRUD, `dedup_key`-keyed, orthogonal to category) + mark/unmark from the WLT-23 ledger popover + a view that groups by `normalizeMerchant`, infers cadence from history (honest "pending" fallback), and shows a `$X/mo · $Y/yr` headline; nav flipped live. Migration 0015 verified on an ephemeral PG. Codex review CLEAN after two BLOCKERs fixed pre-merge: the mark action moved INTO the reused row popover (via a generic `extraActions` slot — it had shipped as a standalone toggle), and Codex's `transaction_flags` RLS suite + gated E2E landed (their handoff; the RLS suite passes in CI's live-PG job + re-verified locally, 24 RLS tests). **CLEAR tied to HEAD `6f58636`. Next: the detection fast-follow** (`SubscriptionDetector` impl — Plaid recurring vs custom, the deferred elicitation). Earlier: **WLT-24 (Subscriptions) brief + architecture APPROVED.** Architecture ([bets/WLT-24/architecture.md](bets/WLT-24/architecture.md)): a `transaction_flags` overlay table (owner-CRUD, `dedup_key`-keyed, orthogonal to category, no composite FK) + a pure `@wealth/core` cadence/total compute (infer from ≥2 occurrences, honest "pending" fallback) + a provider-agnostic `SubscriptionDetector` seam (Plaid-recurring-vs-custom deferred to the detection fast-follow; the Plaid option flagged as a future ADR-002 amendment). Migration 0015. A new top-level Subscriptions surface (fills the WLT-19 nav placeholder) to see + mark recurring subscriptions with a running monthly/annual total. **Manual-first, detect-later** (operator decision): slice 1 = a new owner-CRUD `transaction_flags` overlay table (orthogonal to category — a Netflix charge is both "Entertainment" AND a subscription) + a mark/unmark action reusing the WLT-23 ledger picker + the aggregation view; auto-detection (Plaid `/transactions/recurring/get` behind a swappable adapter, or a custom detector) is a deliberate fast-follow that auto-sets the flag as a signal the user overrides. Shares the substrate with a planned follow-up-flag bet. `architecture_required: true`. Brief at [bets/WLT-24/brief.md](bets/WLT-24/brief.md). Earlier: **WLT-22-5 (Transfers & payments don't count as spending) — app code SHIPPED** (PR #81 server spine `149794c` + PR #83 UI `1e3c1c1`): the budget/recap/anomaly numbers no longer double-count credit-card payments + internal transfers — a protected, undeletable, auto-populated "Transfers & Payments" category (excluded from spend, visible + drillable, user-overridable) driven by a normalized region-pluggable `kind` seam; ~$100k/yr of transfers drops off the operator's totals. Codex-approved (no findings); two nudge ISSUEs fixed pre-merge. Migration 0014 verified on an ephemeral PG. **Verification SHIPPED (PR #85, `7260c3e`):** Codex's RLS suite (undeletable `source='system'`, owner-scoped flag, the composite FK actively rejects a forged cross-tenant assignment) + the gated real-path E2E (transfers + CC-payment excluded, mortgage kept, override survives a CDC revision, second-user isolation) — two test-only defects in the handed-off suite (a transaction-poisoning `asUser` → savepoint-safe; an FK assertion `rowCount 0` → `rejects.toThrow`) fixed before merge; 22 RLS tests pass in CI's live-PG job. **WLT-22-5 SHIPPED; CLEAR tied to HEAD `7260c3e`; WLT-22 bet COMPLETE again.** Earlier: **WLT-22 re-opened → WLT-22-5** — dogfooding the budget after the 1000-row cap fix surfaced a deeper correctness bug: every spending compute counts **every debit regardless of category**, so credit-card payments (the purchase **and** the payment-from-checking) + internal transfers are double-counted (~37% inflation, ≈ $99,980 of ≈ $266,937/yr on the operator's account). Triaged **[INC-2026-06-20](bets/WLT-22/incidents/INC-2026-06-20-spending-double-count/triage.md)** (P1, PR #79); fix designed as a **protected, auto-seeded "Transfers & Payments" category** that doesn't count as spending — visible + undeletable, auto-assigned from Plaid transfer/CC-payment tags (zero day-one friction), user-overridable with the WLT-22-3 "remember" rules. Story + design + copy drafted (`ready`). **Also today:** **FIX-2026-06-20c — the budget read 1000-row cap** (PR #77, `1d70e08`) paginated `readSpendingForBudgets` + the drill + the shared resolver so totals are complete on >1000-txn accounts (built on FIX-2026-06-20b's rule-apply cap fix, PR #76); **sign-in success-message conflict** (PR #78, `0e833f6`) — the welcome state no longer coexists with the challenge prompt (recovered from an orphaned orchestrator commit). Earlier: **WLT-23 (Transactions ledger) bet COMPLETE** — all three slices shipped (PR #66 ledger · #68 filters · #69 in-row recategorize, squash `478a16c`): **see · find · correct** every transaction across accounts, on the WLT-22 saved-category spine. Plus **[FIX-budget-popovers](fixes/budget-popovers.md)** (#67). Earlier the same surface began with **WLT-23-1 (Transactions ledger) SHIPPED** (PR #66, squash `fd1094f`) — the new top-level **Transactions** nav item is live: a searchable, **keyset-paginated**, reverse-chron all-accounts ledger (date · merchant · amount · resolved category · account), read-only, reusing the WLT-22 `saved ?? Plaid` resolver for the category column + the WLT-2 `transactions` read + the WLT-20 shell. Both directions (debits + credits with `+`) with pending flagged; CDC superseded/removed hidden by RLS; `architecture_required: false`. Codex authored the gated owner-isolation E2E (page-boundary pagination + cross-tenant negative); **CLEAR tied to HEAD `76489d6`** (a stale-SHA security re-flag on the cursor was verified already-fixed at HEAD — the reverify-against-HEAD discipline). It delivers the WLT-23 bet's core; optional fast-follows (account/category filters, recategorize-in-list) deferred. **Earlier today: [OPS-2](ops/OPS-2.md)** — migrate-on-deploy is **live** (every merged migration auto-applies to prod via `pnpm migrate` on a post-deploy GitHub Action; WLT-23-1 carried no migration). **WLT-23-2 (Filter the ledger — account + category) SHIPPED** (PR #68, squash `457734b`) — account + resolved-category filters beside the search, composing with keyset paging; Codex caught a real **sparse-filter cursor-stranding BLOCKER** (fixed: bounded auto-continue, never a false "no matches") + gated the "Other" option, and authored the filtered page-boundary + isolation E2E; **CLEAR tied to HEAD `26942ed`**. **Also shipped: budget detail in popovers** ([FIX-budget-popovers](fixes/budget-popovers.md), PR #67) — the drill-down + year-spread no longer expand inside the table. **WLT-23 (Transactions ledger) bet COMPLETE** — all three slices shipped: **WLT-23-1** (the ledger, PR #66) · **WLT-23-2** (account + category filters, PR #68) · **WLT-23-3** (recategorize from the row via the reused WLT-22 popover picker, PR #69). The user can now **see** every transaction across accounts, **find** one (search + filters), and **correct** its category in place — the "see · find · correct" loop, reusing the WLT-22 saved-category spine so the category never disagrees with the budget. Plus **[FIX-budget-popovers](fixes/budget-popovers.md)** (PR #67) moved the budget drill-down + year-spread to popovers. Earlier: **WLT-22 (Budget transparency + category correction) bet COMPLETE** — all three slices shipped: **WLT-22-1** (drill into a category → its real line items, PR #60) · **WLT-22-2** (recategorize + custom categories — the saved-category spine, PR #61) · **WLT-22-3** (remember the merchant — rules that backfill past + apply at sync, PR #62, squash `3d025c5`). The user can now **verify** a budget number, **correct** it (saved across Plaid re-syncs), and **remember** a merchant — every surface (budget/recap/anomaly) reading the user-resolved category through **one shared `saved ?? Plaid` resolver**; a `'user'` override always outranks a `'rule'`. Three owner-CRUD tables (`categories`/`transaction_categories`/`category_rules`, composite FKs, hard-delete). Codex tied each story's **CLEAR to its exact HEAD** (`7d29173`/`b571dee`/`74980a2`) after authoring the load-bearing RLS suites + gated real-path E2Es it owns (CDC-survival, future-sync auto-categorization, cross-tenant isolation). **Deferred follow-on (not this bet):** category/rule management + the rename-safe `budgets.category_id` migration. Earlier: **WLT-21 (Budget & Spending) bet COMPLETE** — both stories shipped: WLT-21-1 (the table, PR #58) + WLT-21-2 (the year-spread chart, PR #59). `/budget` is live in the shell: set a budget per category, with a recommended-from-your-own-history figure, this-month actual, over/under, **and expand any category to a 12-month bar chart** (custom SVG, no charting dep). Two pre-merge catches worth noting: the **live-PG RLS gate caught a real bug** (clearing a budget needs a hard delete — soft-delete-via-RLS is impossible under the owner SELECT policy), and Codex caught the percent-path E2E gap, the "Use this" affordance, a category-taxonomy mismatch, and the responsive month-label contract. Earlier: **WLT-21 (Budget & Spending) bet COMPLETE** — both stories shipped: WLT-21-1 (the table, PR #58) + WLT-21-2 (the year-spread chart, PR #59). `/budget` is live in the shell: set a budget per category, with a recommended-from-your-own-history figure, this-month actual, over/under, **and expand any category to a 12-month bar chart** (custom SVG, no charting dep). Two pre-merge catches worth noting: the **live-PG RLS gate caught a real bug** (clearing a budget needs a hard delete — soft-delete-via-RLS is impossible under the owner SELECT policy), and Codex caught the percent-path E2E gap, the "Use this" affordance, a category-taxonomy mismatch, and the responsive month-label contract. Earlier: **WLT-19 app-shell bet COMPLETE** (PR #57); **WLT-15** engagement bet COMPLETE; **[OPS-1](ops/OPS-1.md)** prod Inngest auto-sync. — Updated 2026-06-17._

## In flight

**WLT-23 — Transactions: a searchable ledger of all your activity — `shipped` (bet COMPLETE — all three slices shipped, 2026-06-19)** — surfaced by the first operator dogfooding the shipped app: _"I'd like to see all the transactions in the account."_ Today you can see **accounts** (balances) and **budgets** (categorized spend) but there is **no plain list of all your transactions** — the only individual-transaction view is the WLT-22-1 drill-down, scoped to one category, current month. The bet: a new **top-level "Transactions" nav item** (sibling to Accounts, not a sub-menu — flat nav, a primary return-to surface) → a `/transactions` page in the WLT-20 shell, a **reverse-chron all-accounts ledger** (date · merchant · amount · **resolved** category · account), **paginated/lazy-loaded** (fast on a full 24mo history), **searchable** (free-text over merchant/description; account/category filters a story decision), **owner-scoped** (RLS), reading the existing WLT-2 `transactions` through the **WLT-22 `saved ?? Plaid` resolver** (so the category column matches the budget) + an additive `transactions_viewed` event. **Read-only this bet** — no editing/manual-add/CSV/bulk/split (the "don't over-build" guardrail); recategorize-in-list reuses the WLT-22 picker *if* included at all, else a fast-follow. **`architecture_required: false`** — no new schema, no re-sync, all on shipped patterns. Brief `c330acd`.
- **WLT-23-3 (Recategorize from the ledger) — `shipped`** (PR #69, squash `478a16c`, 2026-06-19, the bet's final slice) — each ledger row's category cell is the **WLT-22 `CategoryPicker`** opened **as a popover** (move · create-category · "remember this merchant"), reusing the saved-category spine + the AAL2 routes + events; the only new data is exposing the row's `dedup_key`. On success the ledger **reconciles** (single move updates the row in place + drops it if it no longer matches an active category filter; a rule refetches the page). **E2E coverage accepted as-is** (the recategorize write path is already E2E-proven by WLT-22; the delta is UI + reconcile, component-tested) — no near-duplicate browser E2E. Two self-caught CI failures first (test-mock typecheck → lint, from pushing without the full local gate). **CLEAR tied to HEAD `060d2b1`.** 243 unit/component green. _Jira mirror skipped — no connector (logged)._
- **WLT-23-2 (Filter the ledger — account + category) — `shipped`** (PR #68, squash `457734b`, 2026-06-18) — an **account** filter (SQL `.eq`, uuid-validated) + a **resolved-category** filter (agrees with the ledger column + the budget) beside the shipped search; all three **AND** server-side, composing with keyset paging; changing any resets to page 1; honest no-match + **Clear**; `transactions_filtered` (fire-and-forget, on filter change only). **Load-bearing impl:** the resolved-category filter resolves **per-row** through a **bounded over-fetch-and-filter keyset scan** (cap `20×PAGE_SIZE`, cursor = last row scanned) — chosen over SQL-set interpolation to keep the category name + `dedup_key` out of the `.or()` grammar (the cursor-injection lesson). **Codex caught a real BLOCKER** — a sparse filter could strand matches behind the scan-cap cursor → fixed with a **bounded client auto-continue** + a "keep looking" state (never a false "no matches"); plus an ISSUE (the "Other" option now gated on a `hasOther` probe). Codex authored the gated E2E (filtered page-boundary paging exactly once + second-user isolation). **CLEAR tied to HEAD `26942ed`.** 241 unit/component green + the gated E2E. _Jira mirror skipped — no connector (logged)._
- **WLT-23-1 (The Transactions ledger) — `shipped`** (PR #66, squash `fd1094f`, 2026-06-18) — the nav item + the owner-scoped, **keyset-paginated** (over `occurred_on desc, id desc` — stays under the 1000-row PostgREST cap, the app's first deliberately-unbounded list), **searchable** (merchant/description), all-accounts list (date · merchant · amount · **resolved** category · account name). Shows **both directions** (debits + credits with a `+`, accessible "credit" label) with **pending** flagged; CDC superseded/removed hidden by RLS. Category via the WLT-22 `effectiveCategory` resolver. Honest states (loading · 3 empties incl. the connect-nudge · error+retry). `transactions_viewed` event. Read-only. **Codex caught 2 findings**: the gated owner-isolation E2E (its own deliverable — authored `e2e/transactions.spec.ts`, `5e46375`, covering page-boundary pagination + cross-tenant negative) + a cursor input-hardening ISSUE (engineer-fixed `150aa37` — strict date/uuid validation before the `.or()` filter). A follow-on security re-flag cited a **stale SHA**; verified already-fixed at HEAD. **CLEAR tied to HEAD `76489d6`.** 235 unit/component green + the gated E2E. _Jira sub-ticket mirror skipped — no connector on host (logged)._

**WLT-22 — Budget transparency + category correction — RE-OPENED for `WLT-22-5` (2026-06-20)** — the bet shipped its original three slices, but dogfooding the budget after the 1000-row cap fix exposed a correctness bug in the *same domain* (category-resolved spending): transfers + credit-card payments are counted as spending → ~37% inflation. New slice added on the existing saved-category spine.
- **WLT-22-5 (Transfers & payments don't count as spending) — `merged` (app code shipped 2026-06-21; RLS/E2E follow-up pending)** — a protected, undeletable **"Transfers & Payments"** category (`source='system'`, new `counts_as_spending=false`) auto-assigned from the normalized **`kind`** seam (AC8 — classified in the Plaid adapter from the *detailed* category, so a CC-payment is split from a mortgage; core never branches on Plaid strings → region-pluggable); a `countsAsSpending` predicate threaded into all four `packages/core/budget.ts` computes **+ recap + anomaly** (the AC4 cross-surface guardrail, guard-tested) so the totals/year-spread/recommendation drop the ~$100k/yr; user can move any transaction in/out (a `'user'` override wins + survives CDC re-sync) with WLT-22-3 "remember" rules; a visible non-spending group on `/budget` + a per-user, no-flash dismissible review nudge. Migration `0014` (expand-only, OPS-2 auto-applied; verified end-to-end on an ephemeral Postgres). Shipped in two PRs: **#81** server spine (squash `149794c`) + **#83** UI (squash `1e3c1c1`; Codex-approved, no findings — two nudge ISSUEs fixed in `53dc829` first). #82 was the pre-rebase UI PR, superseded after #81's squash-merge. **Outstanding before `shipped`:** Codex's RLS suite (undeletable `source='system'`; can't forge `counts_as_spending`; isolation) + gated real-path E2E (transfers + CC-payment excluded, mortgage kept, override survives re-sync) — target the merged 0014 schema on `main`.

**WLT-22 — Budget transparency + category correction — `shipped` (bet COMPLETE, 2026-06-17)** — surfaced by the first real user on shipped WLT-21: _"I can't tell if the numbers are correct — Rent + Utilities are grouped, so I don't know what was counted as rent."_ The fix in two layers: **(1) drill into a category → see its real line items** (verify; uses data we already store, no re-sync), and **(2) user-owned categories + correction** — the user's own categories are the **source of truth**, Plaid demoted to a **cold-start seed**; recategorize via per-transaction overrides + "remember the merchant" rules. **Data-model spine (designed with the user):** Plaid owns the entries + stays the category *indication*; **what the user sets is SAVED** — a per-transaction assignment in `transaction_categories` keyed by the stable `dedup_key` (survives CDC re-syncs) + a `category_rules` mapping that **writes** its matches (past on create, future at sync); reads = **saved `??` Plaid** via one shared helper (so budget/recap/anomaly never disagree). The ownership reframe **drops the heaviest risk** — the Plaid `detailed` backfill is now optional. Sliced (re-sliced at story time): **WLT-22-1** (drill-down — no schema — **shipped**) → **WLT-22-2** (recategorize + custom categories — the saved-category spine + manual correction — **shipped**) → **WLT-22-3** (remember-the-merchant rules — automation on the same spine — **shipped**, the bet's final slice). **Bet COMPLETE** — all three shipped.
- **WLT-22-1 (Drill into a category — verify the number) — `shipped`** (PR #60, squash `d09d69a`, 2026-06-17). Tap a category's "This month so far" amount → the **real transactions** behind it (date · merchant · amount), summing to exactly the number shown (the load-bearing honesty contract). **No schema / no category model** — a read of existing owner-scoped transactions + a UI; lazy-loaded on open with honest loading/empty/error states. **Cross-model Codex caught 4 things across 1 round** — a DB-error-as-empty-success masking the error state (AC3), the missing owner-isolation E2E negative case (AC4), a drill funnel double-counting on retry (AC6), and a non-semantic panel (labelled region + `<th scope>` headers); engineer fixed the 3 app-code findings, **Codex authored the E2E negative case** (kept independent of the implementing host). **CLEAR tied to HEAD `7d29173`**. Unit suite 190 green + the gated real-path E2E (drill reconciles through session→RLS→render; a second user can't read user 1's rows).
- **WLT-22-2 (Recategorize a transaction + custom categories) — `shipped`** (PR #61, squash `a7d95b0`, 2026-06-17). From the WLT-22-1 drill-down, **move a mis-tagged transaction** → right category, **creating a custom category inline** to split a lumped group. **SAVED** keyed to the stable `dedup_key` (survives Plaid CDC re-syncs), read through **one shared resolver** (`saved ?? Plaid`) wired into budget/recap/anomaly (pure compute unchanged); Plaid stays the cold-start default. Lands the spine: `categories` + `transaction_categories` (owner-CRUD RLS, composite FK, hard-delete), the resolver + AC4 guard test; **name-keyed → no budgets migration** (WLT-21 budgets carry over free); `computeRecommendedBudgets` reads `category.kind`. **Approved scope re-slice:** merchant rules + category rename/delete + the rename-safe `category_id` migration all deferred. **Codex caught 2 BLOCKERs — both its own test deliverables** (RLS suite + real-path E2E), routed back per the cross-model independence principle + authored in `b571dee`. **CLEAR tied to HEAD `b571dee`**. 207 unit tests green + the gated RLS suite & E2E (recategorize/reconcile, CDC survival, second-user isolation).
- **WLT-22-3 (Remember the merchant — category rules) — `shipped`** (PR #62, squash `3d025c5`, 2026-06-17). In the recategorize picker, **"Always categorize [merchant] this way"** → a `category_rules` mapping that **writes `'rule'` assignments** to all the user's matching transactions (**backfill past** on create) and is **applied at sync** to new ones (**future**, via `applyAllRulesForUser` in the post-ingest `apply-category-rules` step). A `'user'` override always outranks a `'rule'`. Reads unchanged (a `'rule'` row is just a saved assignment the resolver already resolves). Lands the **3rd table** (`category_rules`, owner-CRUD + composite FK, hard-delete), a shared pure `normalizeMerchant`/`matchRuleAssignments`, and the counted "updated N transactions" UI. **Codex caught 2 BLOCKERs — both its own test deliverables** (RLS suite + real-path E2E), routed back + authored in `74980a2` (remember → backfill 3, override survives, sync auto-categorizes a new txn, second-user isolation). **CLEAR tied to HEAD `74980a2`**. Plus 3 UI-polish commits (roomier create-card, consistent pop-up spacing, Cancel-as-link). 214 unit tests green + the gated RLS suite & E2E. **Rule management (view/edit/delete) deferred** to a follow-on. **Completed the bet.**
- **WLT-22-4 (Robust merchant identity for rules) — `ready`** (2026-06-19, hardening follow-on) — after **INC-2026-06-19** (the fuzzy-match fix, PR #70) the user flagged that name-fuzzing is inherently imperfect. WLT-22-4 layers **Plaid's stable `merchant_entity_id`** (the provider's canonical merchant identity) as the **primary** match key, with the fuzzy name as **fallback** — so "remember the merchant" auto-categorizes new transactions even when Plaid's display name varies, exactly + with no over-match. Backend only (ingest + a column on `transactions` + `category_rules` + entity-first matching); **expand-only migration** (auto-applies via OPS-2). The irreducible floor (no entity **and** no name) stays unmatchable. design/copy `n/a` (no UI). **Next:** `/build WLT-22-4` (Codex owns the RLS + entity-match gated E2E). _Jira mirror skipped — no connector (logged)._

_Also available, not started: the remaining section features (Goals · Debt payoff · Investments · Subscriptions — each mounts into the shell via the WLT-20 contract); observe the budget engagement signals (`budget_set` / `budget_spread_viewed`)._

**WLT-21 — Budget & Spending (the first section feature) — `shipped` (bet COMPLETE, 2026-06-16)** — brief + architecture `approved` (2026-06-16); **two stories, both shipped**. Turns the passive "where it went" recap into an **active budget-vs-actual control surface** the user sets + returns to, realizing the reserved `budget_guardrail` archetype at page scale. Decisions: **recommended = the user's OWN history** (no income captured → no fabricated 50/30/20 anchor; median of trailing monthly totals + a modest discretionary trim); **bank's real categories** (Plaid primary, union'd with the legacy/documented names; finer split deferred); **sliced** — the table first, the year-spread chart second. New owner-CRUD `budgets` table (the intents posture, **not** the financial service-role posture) — the first user-writable financial-adjacent table, so cross-tenant isolation was load-bearing (RLS suite + a gated real-path E2E). On-read compute (no rollup/job); **no new dependency** (the year chart is hand-drawn SVG). **Story numbering is now nested under the bet — `WLT-<bet>-<n>`.**
- **WLT-21-1 (The Budget & Spending table) — `shipped`** (PR #58, 2026-06-16). `/budget` live in the shell: a per-category table (Category · Recommended · This month so far · Your budget [$ or %] · over/under), rows = your real spending categories + an "+ Add a category" picker, honest cold-start ("—"). New owner-CRUD `budgets` table (`0010`); pure compute in `packages/core/budget.ts` (median-of-trailing-months recommendation, discretionary trim, percent→effective-cap); on-read, no rollup, no new dependency. **Cross-model Codex caught 3 things across rounds** — the missing percent-path E2E, the unrendered "Use this" affordance, and a taxonomy mismatch (GROCERIES/INSURANCE) — Security clean. **The live-PG RLS gate caught a real pre-merge bug:** an authenticated soft-delete (`update … set deleted_at`) fails Postgres' UPDATE WITH-CHECK once the row leaves the `deleted_at is null` SELECT policy → clearing a budget hard-deletes instead (reproduced on real PG, the schema simplified to a plain `unique(user_id,category)`). 183 tests + the budgets RLS suite (owner CRUD incl. hard-delete clear + cross-tenant deny) + a gated real-path E2E (set $ then % → reload persists).
- **WLT-21-2 (Year-spread view) — `shipped`** (PR #59, 2026-06-16). Each budget row **expands to a 12-month bar chart** of that category's monthly spend (elicited: expand-on-demand). **Custom SVG, no charting dependency** (a lib would trip the foundational-stack deviation gate — declined); `computeMonthlySeries` on-read + shipped with the page (no rollup/endpoint); current month marked "so far", honest real-zeros, a visually-hidden data table as the SR equivalent (AC5), `budget_spread_viewed` instrumentation. Codex caught + fixed the responsive month-label contract (single-initial phone / 3-letter desktop) pre-merge. **Completed the bet.**

**WLT-19 — The app shell (responsive nav frame + "Coming soon") — `shipped` (bet COMPLETE, 2026-06-15)** — brief + architecture `approved` (2026-06-15); **one story, the whole frame end-to-end**. Surfaced by the user's request to make the landing Dashboard a full-screen, cross-surface app with a persistent left nav, building each section "this week." Scope (PM DRI decision): **the shell + nav + "Coming soon" stubs ONLY** — the six sections are separate feature bets — so build-the-frame-once unblocks the week. **No foundational-stack deviation** (Headless UI + Heroicons are a small Tailwind-family addition; `@wealth/ui` hand-rolls its dialogs).
- **WLT-20 (The app shell) — `shipped`** (PR #57, 2026-06-15). A Next.js `(app)` route group + one shared `layout.tsx` that **enforces AAL2 once** (the load-bearing AC3) and renders `<AppShell>`; a single `NAV_SECTIONS` source-of-truth drives the sidebar, the mobile drawer's copy of it, the honest `<ComingSoon>` stubs, **and** — via the edge-safe `SHELL_PATHS` — middleware protection (one source, no drift). Dashboard (recap + workflow) + Accounts moved under the shell (old `/settings/accounts` → `/accounts`); `/settings/security` reached from the account menu. Headless UI `Dialog` drawer (focus-trapped, Esc/scrim/nav-tap close, focus-return, reduced-motion) + `Menu` account control (Security · Sign out). **AC3 proven in CI** — `e2e/auth-guard` redirects every one of the 8 shell routes + the moved Accounts URL when un-AAL2; `[mechanical-output-verification]` on the manifest + middleware. Tests: nav/`SHELL_PATHS` integrity + `isActiveNav` (unit), `NavItem`/`ComingSoon`/`AccountMenu`/`MobileDrawer` (jsdom). Cross-model Codex review caught a real BLOCKER — the missing **authenticated** real-path E2E (the shell renders + drawer/menu happy path under a real AAL2 session) → added `e2e/shell-flow.spec.ts` (gated passkey lane) + fixed four gated specs the reframe had broken (the old dashboard chrome moved into the account menu); **clear** on re-review.

**WLT-15 — "Your money since last time" (the reason to return) — `shipped` (bet COMPLETE, 2026-06-15)** — brief + architecture `approved` (2026-06-14); **all three stories shipped — the four "since last time" signals are live**. The post-MVP **engagement/retention bet** — turns WAWU + Day-30 from theoretical into real by making the dashboard different every visit. Surfaced by dogfooding: the first real user hit the dead-end ("nothing left to do" after setting a target). Builds entirely on the shipped loop (WLT-2 data + 24mo history, WLT-4 workflow/target, WLT-5 funnel) — **no new data sources, no foundational-stack deviation**. Four signals (net-worth movement · spending vs last week · progress toward target · high-precision anomalies), each visit surfacing **one** prompted action → a repeatable `WorkflowRun` (WAWU). Architecture: a new `net_worth_snapshots` time-series (daily Inngest sample — balances can't be derived from txns) + the already-modeled `Anomaly` entity (rules-based, dismissible) + new funnel/metric panels. **Sliced:** computable signals + one action first, then the anomaly engine.
- **WLT-16 (The "since last time" recap — movement + target progress + one move) — `shipped`** (PR #47, 2026-06-15, live in prod behind `RECAP_ENABLED`). The slice that closes the dead-end + proves the return mechanic: net-worth **movement** since last time + **progress toward target** (wires the WLT-12 "running" workflow to actually track) + **one** honest prompted action → a **weekly-repeatable** `WorkflowRun` (WAWU) + `recap_viewed` return instrumentation → a `/admin/metrics` return panel. Built the core new infra (`net_worth_snapshots` daily-sampled series + `netWorthSnapshotDaily` cron; `complete_recap_action` atomic + weekly-idempotent via a `period` split that leaves the once-ever `target_set` guard intact). 135 tests incl. live-PG (validated against a real Postgres); cross-model Codex review **clear**. **Spending (WLT-17) + anomalies (Slice 2) remain out of this story.** Ships dark until the snapshot job has ≥1 cycle to anchor movement.
- **WLT-17 (Where your money went — spending vs. last week) — `shipped`** (PR #49, 2026-06-15, live in prod behind `RECAP_ENABLED`). The recap's second "what changed" signal + the deferred `spending_snapshot`: total spent this week vs last + top categories, a **display section** in the RecapCard. Pure read + computation over existing `transactions` — no new tables/job/route; **display only, no second action**. Cross-model Codex review caught a real-path-coverage BLOCKER (the owner-scoped spending read had no #36-class E2E) → resolved with `e2e/recap.spec.ts` (real session → RSC → RLS → rendered rows) before the clean re-review.
- **WLT-18 (Anomalies — "something worth your attention") — `shipped`** (PR #51, 2026-06-15, live in prod behind `RECAP_ENABLED`). The final slice (the architecture's Slice 2): a high-precision, dismissible "worth a look" (large charge vs. own baseline / bill due soon / low balance) surfaced as the recap's one action (**Review it** = WAWU; **Dismiss** = quiet). `anomalies` table (owner-SELECT, status-only UPDATE via trigger, service-role INSERT, composite FK, dedup) + daily `anomalyScanDaily` + the anomaly-aware ranker + a dismiss-rate panel. **All three rules** (`large_charge` + `recurring_due` strict + `low_balance`); rules-based (statistical deferred); no PII. 165 tests; **3 Codex rounds** (scope drift → recurring_due implemented; missing real-path E2E → added; missing AC6 event assertions → added) + Security **clean**. Its prod deploy **auto-registered `anomaly-scan-daily`** in Inngest — the OPS-1 auto-sync fix, proven live.

_Other open follow-on tracks (team's choice, not critical path): WLT-4's remaining 5 archetypes (one story each on the shipped engine); the rest of the deferred post-MVP items in `portfolio.md` (marketplace, billing); and Observe (let real traffic populate the instrument panel → `/measure`)._

**WLT-5 — TTFV + WAWU instrumentation — `shipped`** (2026-06-12). The last MVP bet, delivered in one story (WLT-13). Compute + surface what the loop already emits (no new events; the 17-event contract frozen). **Loop now measures itself.**
- **WLT-13 (The instrument panel) — `shipped`** (PR #34, 2026-06-12, live in prod). `0007` read-only views (TTFV full-loop clock + p80 + split times · weekly WAWU · funnel conversion; SELECT revoked from authenticated/anon — Postgres views bypass base-table RLS, so the revoke is the boundary) · `/admin/metrics` (**AAL2 + `ADMIN_EMAILS` allow-list, every unauthorized state → 404 via `getAal2UserId()`+`notFound()` — unenumerable**) · `metrics-snapshot.mjs` → `docs/metrics/` for `/measure` · honest small-n (n everywhere, no-KR-verdicts banner). First baseline recorded: **WAWU=1**. Cross-model Codex **Approve** + Security **clean** after 3 rounds (AC11 route+no-PII E2E → AC5 signed-out redirect→404); gate E2E 3/3 live.

**WLT-4 — Workflow engine + pre-built workflows** — brief + architecture `approved` (2026-06-11); **building**. The MVP-loop **convergence point**: consume the declared `Goal` (WLT-3) + real data (WLT-2) → auto-assemble a personalized running workflow → surface one platform-prompted action (`WorkflowRun` = the WAWU north-star unit). Scope (PM-elicited): **template-select + personalize** (composition/marketplace deferred); **one workflow per Goal.kind** via a ~6-archetype registry mapping all 14 goalKinds (no dead-end). Architecture: 2 tables (`workflows`/`workflow_runs`), composite same-user FKs, immutable runs, two-phase assembly, no new tooling.
- **WLT-12 (Assemble + run your first workflow — net-worth snapshot) — `shipped`** (PR #31, 2026-06-11, live in prod). **The loop-closing story.** The engine end-to-end + `networth_snapshot` (5 goalKinds, balances-only): declare → plan-ready connect bridge → real net-worth snapshot → one-tap "set your target" → an **immutable `WorkflowRun` (the first WAWU event)** → "Running — tracking toward {target}". Engine: archetype registry + two-phase assembly + injectable `EngineStore` seam; the action commits via an **atomic SECURITY-INVOKER plpgsql function** (replay-guarded at two layers); `workflow.assemble`/`workflow.action` audit events; `workflow_assembled`/`action_completed` funnel events. Cross-model: 4 Codex rounds (funnel contract → loading state → engine tests/replay → **atomicity** + audit → E2E), Security **clean**. **Full-path E2E verified live** (19.5s, real stack). Other 9 goalKinds keep the WLT-11 placeholder until their archetype story (total-coverage = bet exit gate).
- **Later slices:** `savings_rule` · `spending_snapshot` · `budget_guardrail` · `cashflow_forecast` · `debt_payoff` (one per story, same engine) · scheduled execution (the "running" fast-follow).

**WLT-3 — Intent-first onboarding** — brief `approved`; **building**. Directive baked in: **intent-first, user-first** — declare intent *before* connecting (defer friction).
- **WLT-11 (Declare your intent — 6-cluster front door) — `shipped`** (PR #26, 2026-06-09, live in prod). The intent-first front door (Fear/Goal/Confusion/Control/Habit/Aspiration → starter intents) → persist `Intent` + derived `Goal` → "putting your plan together" placeholder → bridge to connect; `intent_declared` baseline event. Post-auth now lands here (session-scoped explore escape). Cross-model Codex code + security both **Approve** after 4 rounds — deepest catch: a **composite FK** `goals(intent_id,user_id)→intents(id,user_id)` blocking forged cross-tenant goal→intent links at the DB. Ships without WLT-4.
- **Later slices:** free-text intent expression · intent management (edit/add) · richer Goal params.

**WLT-2 — Account aggregation + CSV fallback** — brief + architecture `approved`; **building**.
- **WLT-9 (connect first bank via Plaid OAuth + initial sync) — `shipped` + activated in production** (PR #18). The `@wealth/aggregation` **pluggable** pipeline (provider-neutral core + Plaid adapter + Supabase Vault + Inngest backfill + owner-SELECT RLS + consent/accounts UI); cross-model Codex code + security both **Approve**. **Validated live** with a real Wells Fargo connection (real accounts + 154 transactions). Prod activation surfaced + fixed: Inngest config + app sync, an atomic link-rollback + Inngest preflight gate (PR #20), and a prod-DB cleanup (test-user cruft + a typo-account dupe purged).
- **WLT-10 (full-history backfill + webhook-driven sync) — `shipped`** (PR #28, 2026-06-11, live in prod). `days_requested:730` (24mo) + a **verified Plaid webhook** (ES256 JWT/JWK + body-hash + replay + ownership re-derivation) → debounced incremental re-sync, **6h cron fallback**, and an **"Importing your history…"** UI that settles on a server `history_synced_at` flag stamped only when sync activity **stabilizes** (consecutive quiet passes — not a clock). Cross-model Codex code **Approve** + Security **clean** after 6 rounds (premature-Connected → clock → unconditional-stamp → single-pass → consecutive-quiet). **Ops to light up real-time:** set `PLAID_WEBHOOK_URL` in Vercel (Production) + register with Plaid — cron completes full history until then.
- **Later slices:** re-auth/connection-health UI · CSV / email import · Statements (>24-month history) · 2nd provider (KR2).
- **Ops — resolved 2026-06-14:** prod Supabase **Site URL** → `https://home-app.kindtree.us` (TOTP issuer + email/reset links correct); **`PLAID_WEBHOOK_URL`** set (real-time webhook sync live); **`ADMIN_EMAILS`** set (the `/admin/metrics` panel is now reachable). _Minor leftover: two old Plaid items un-revoked on Plaid's side — cosmetic._

WLT-6 + **WLT-7 — authenticator-app (TOTP) backup factor — `shipped`** (2026-06-07). **WLT-8** (support-gated recovery, both-factors-lost) is **parked** by decision: revisit once WLT-7 is in real use and backup-adoption is measured — not auto-queued.
- **WLT-14 (Forgot password — self-serve reset) — `shipped`** (PR #45, 2026-06-14, live in prod). Surfaced by dogfooding: a locked-out user had **no way back** (the first real user needed an admin reset). "Forgot password?" → email link → `/reset` → sign in; recovers the **first factor only** (passkey AAL2 still applies; both-factors-lost stays the parked WLT-8). **Anti-enumeration** (identical response/UI for any email) + rate-limited; PKCE recovery code exchanged via a callback route (RSCs can't persist cookies — the #36 constraint); `@vc1023/passkey-2fa` **0.4.0**; `password_reset_*` audit trail. Cross-model Codex **Approve** + Security **clean** (1 round — AC2 redirectTo). _Follow-up landed 2026-06-15: the full real-path recovery-flow E2E (`e2e/forgot-password.spec.ts`, Codex; `E2E_PASSKEY=1`-gated)._
- **Later slice:** change-password while signed in (shares `updatePassword`; a thin fast-follow).

## Next up (from `docs/foundation/plan.md`)

_The MVP portfolio is delivered — these are **follow-on tracks**, the team's choice, not a critical path:_

| Track | What | Why |
|-----|-------|----------|
| **Observe** | let real traffic populate the instrument panel → run `/measure WLT-5` weekly | the baselines (TTFV/WAWU/Day-30) decide whether the foundational bet is validated — read before building more |
| WLT-4 breadth | the remaining 5 archetypes (`savings_rule` → `debt_payoff`), one story each on the shipped engine | every declared intent gets a matching workflow (total-coverage test = bet exit gate) |
| Post-MVP bets | anomaly detection · marketplace · billing (`portfolio.md` "deliberately out of MVP") | promote via `/create-brief <free-text>` when the loop's baseline justifies the next wedge |

**Loop status: ①②③④⑤ — the MVP bet portfolio is COMPLETE** (shipped ~3 weeks ahead of the ~2026-07-01 forecast). The loop runs and measures itself.

## Awaiting human approval

_None._ **WLT-19 — the app shell — brief + architecture both `approved` (2026-06-15); first story WLT-20 `ready`** for `/build`. The navigational frame the week's six feature bets mount into: a `(app)` route group with one shared layout that renders the responsive shell (fixed sidebar → hamburger drawer on mobile, account menu) + enforces **AAL2 once for every shell route**, driven by a `NAV_SECTIONS` config (the mounting contract: flip `coming_soon→live` + add a page = zero shell rework). Elicited: **Headless UI + Tailwind** + **collapsible drawer**. The five not-yet-built sections render honest `<ComingSoon>`. Build-gate caution baked into WLT-20 AC3: **no shell route may render without AAL2** (real-path E2E across every route). Next: `/build WLT-20`.

## Recently shipped

- **WLT-13 — The instrument panel (TTFV + WAWU + funnel)** — `shipped` 2026-06-12 to production (PR #34); **the final bet of the MVP portfolio**. Read-only Postgres views over the loop's events compute **TTFV** (signup→action, p80 vs 180s, split times), **weekly WAWU** (the north star — first reading: 1), and **stage funnel conversion**; an AAL2 + allow-list `/admin/metrics` page renders them (every unauthorized state → 404, unenumerable; aggregates only, n everywhere, no PII), and `metrics-snapshot.mjs` records dated `docs/metrics/` snapshots for `/measure`. View SELECT revoked from `authenticated`/`anon` (views bypass base-table RLS → the revoke is the boundary). 94 tests + a 3-case live gate E2E; Codex **Approve** + Security **clean** after 3 rounds. **Makes the foundational hypothesis falsifiable (KR4/KR5/KR3).**
- **WLT-12 — Assemble + run your first workflow (engine + net-worth snapshot)** — `shipped` 2026-06-11 to production (PR #31). **The loop-closing story**: a declared `Goal` + real balances auto-assemble into a personalized workflow with one platform-prompted action; completing it writes the **first immutable `WorkflowRun` — the first WAWU event**. Archetype registry (`networth_snapshot`, 5 goalKinds) + two-phase assembly (select at declare → personalize post-connect) + an **atomic SECURITY-INVOKER action commit** (replay-guarded at two layers: in-transaction check + `unique(workflow_id, kind)`) + audit trail (`workflow.assemble`/`workflow.action`) + funnel events. 86 tests incl. live-PG RLS (forged composite-FKs both directions, run immutability, the RPC driven as an authenticated user); **full-path E2E verified live** (19.5s). 4 Codex rounds + Security clean — deepest catch: the **non-atomic action commit** whose half-failure would have stranded workflows un-recoverably behind the replay guard.
- **WLT-10 — Full-history backfill + webhook-driven sync** — `shipped` 2026-06-11 to production (PR #28); deepens WLT-2 from the recent ~90 days to the full **24 months** + keeps it fresh. `days_requested:730`; a **verified public Plaid webhook** (ES256 JWT vs Plaid JWK cached by `kid` + body-sha256 + `iat` replay-guard + item→connection ownership re-derivation) → **debounced** incremental refresh; **6h cron** missed-webhook fallback; `needs_reauth`/`error` health + `CONNECTION_ERROR` funnel signals. The **"Importing your history…"** UI derives from a server `history_synced_at` flag stamped only when sync activity **stabilizes** (consecutive quiet passes, via the pure unit-tested `settleHistory`) — survives reload, correct in both directions. 60 tests; cross-model Codex **Approve** + Security **clean** after 6 rounds. **Real-time webhooks pending one ops step:** `PLAID_WEBHOOK_URL` in Vercel (cron completes full history until set).
- **WLT-9 — Connect first bank via Plaid OAuth + initial sync** — `shipped` 2026-06-08 to production (PR #18), **activated** (prod Plaid keys live, Production-only scope); the first WLT-2 aggregation story and the moment the loop touches **real money**. Built the `@wealth/aggregation` pluggable pipeline (provider-neutral core → never imports Plaid; isolated Plaid adapter; Supabase Vault token store — 0 tokens in tables; Inngest 90-day backfill, cursor-after-commit; owner-SELECT-only RLS, service-role writes; consent → unwrapped Plaid Link → connected-accounts → disconnect). 40 tests (+ live-PG RLS), cross-model Codex code + security review both **Approve**. Prod deploy in flight; **production Plaid keys pending** (ships dark via `check-env` until set).
- **WLT-7 — Authenticator-app (TOTP) backup factor** — `shipped` 2026-06-07 (PR #12). Optional TOTP second factor + sign-in fallback; closes the passkey-lockout DRI risk. Codex code + Security review clean (cross-model); 3 review rounds + an independent package audit (fixed credential-table RLS + signing-key hardening). Published as **`@vc1023/passkey-2fa@0.3.0`** (password + passkey + authenticator).
- **`@vc1023/passkey-2fa` published to npm** — 0.1.1 → 0.2.0 → **0.3.0**; reusable password + passkey + authenticator(TOTP) 2FA for Next.js + Supabase, extracted from WLT-6.
- **WLT-6 — Sign up with passkey MFA + sign in** — `shipped` 2026-06-06 to production (`home-app.kindtree.us`); PR #2; Codex review + Security review approved; full passkey E2E green.
- **Foundational architecture bet** — `approved` 2026-06-05 (+ **ADR-001**: passkey via custom WebAuthn, TOTP as Supabase-native fallback).
- **MVP bet portfolio + plan + product + research + architecture** — all `approved` 2026-06-05.

## Blockers

_None._

## Risks

- **Optional TOTP backup (WLT-7)** — users who skip the nudge stay single-factor (passkey-loss lockout persists) — likelihood: med / impact: med — mitigation: prominent nudge + WLT-5 backup-adoption metric; WLT-8 support recovery is the floor — owner: PM
- **Republish `@vc1023/passkey-2fa` 0.2.0 → 0.3.0** for WLT-7 (TOTP added) while home-app consumes via workspace — likelihood: low / impact: med — mitigation: bump + republish only after build green — owner: Engineer
- **Self-built auth surface on a financial app** (custom WebAuthn + now TOTP/factor-removal) — likelihood: med / impact: high — mitigation: AAL2-gated, server-side verify, rate-limited, mandatory Security Review per build — owner: Security
- **PR previews don't fully exercise auth** — passkeys are domain-bound (`home-app.kindtree.us`); preview URLs render but auth completes only locally/prod — likelihood: low / impact: low — mitigation: local E2E + prod verification

## Health

- **Throughput:** WLT-6 built → reviewed → shipped → extracted into a published npm package → hardened, across 2026-06-05…07.
- **CI:** `check` + `e2e` green on main (lint, typecheck, live RLS, build-registration, prod audit gate, guard E2E).
- **Plan freshness:** `last_refreshed: 2026-06-05` — consider `/plan` refresh after WLT-7 ships.
- **Tooling note:** GitHub via `gh` CLI (PRs #2–#10 used); Jira MCP not connected (mirrors skipped, logged in DRIs).
