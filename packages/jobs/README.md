# @wealth/jobs

Inngest durable functions — aggregation sync, scheduled anomaly scans. Runs off
the request path to protect the Reliability fitness function (RPO ≤ 5 min). See
`docs/foundation/architecture.md` → Boundaries + Layer 4 (Ops stack).
