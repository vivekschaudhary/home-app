# @wealth/aggregation

Account-aggregation provider adapters (Plaid and others) + Supabase Vault token
access. Multi-provider with CSV-import fallback. Tokens are envelope-encrypted in
Vault, never plaintext. See `docs/foundation/architecture.md` → DRI Risk R2.
