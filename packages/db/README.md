# @wealth/db

Supabase client, generated schema types, and RLS policy helpers. Tenancy is
default-deny RLS keyed on `auth.uid()`. Migrations live in `/supabase`. See
`docs/foundation/architecture.md` → Foundational Data Model + Layer 3 (Data).
