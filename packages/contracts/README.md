# @wealth/contracts

OpenAPI specification + generated client/types. The open REST contract serves
the developer/SDK persona (~5%) and the marketplace/B2B packages. See
`docs/foundation/architecture.md` → Layer 2 (Backend stack).
