---
name: dashboard
description: Generate the single-file `docs/dashboard.html` — a browser-renderable view of all living Compass artifacts (foundation, plan, portfolio, scan reports, metrics, status). Auto-invoked by /scan, /metrics, /plan, /status. Shareable as an attachment; opens via file://.
---

Execute the workflow at `compass/workflows/dashboard.md`.

Read that workflow file now and follow it step by step. Load the Project Manager role context (`compass/roles/project-manager.md`). No HITL gate — dashboard is a living artifact, regenerated on demand.

$ARGUMENTS
